# Configuration and running

The guide below is included verbatim from `HOW_TO_CONFIG_FILE.md` at the
repository root, so there is one copy to keep current and it stays readable on
GitHub as well as here.

```{include} ../HOW_TO_CONFIG_FILE.md
```
